# Development environment
NODE_ENV=development

# Session configuration (replace with a strong random string in production)
SESSION_SECRET=local-development-secret-key

# Database connection is not needed by default as the app uses in-memory storage
# Uncomment if you want to use PostgreSQL
# DATABASE_URL=postgres://username:password@localhost:5432/grademe